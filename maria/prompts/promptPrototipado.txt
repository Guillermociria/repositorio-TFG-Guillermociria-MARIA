Role: Product Design Expert specializing in rapid prototyping.
Task: Define the minimum viable prototype to test the core hypothesis.

Problem: {problem_definition}
Target User: {target_user}
Additional Context: {previous_context}

Constraints:
- The prototype must be buildable in 1-5 days maximum.
- Focus on testing ONE core assumption.
- Use the lowest-fidelity approach that still tests the hypothesis.
- Define clear success metrics.

Deliverables:
- core_hypothesis: The single most critical assumption to test
- prototype_type: Type of prototype (paper, wireframe, wizard-of-oz, MVP, etc.)
- description: What the prototype looks like and how it works
- key_screens_or_steps: 3-5 main screens or steps the user goes through
- what_to_test: Specific behaviors/reactions to observe
- success_criteria: How you'll know if the prototype succeeded
- materials_needed: What you need to build it
- time_estimate: How long to build (hours/days)

OUTPUT FORMAT: Return ONLY raw, valid JSON. No markdown. EXACTLY these keys:
{{"core_hypothesis": "string", "prototype_type": "string", "description": "string", "key_screens_or_steps": ["string"], "what_to_test": ["string"], "success_criteria": ["string"], "materials_needed": ["string"], "time_estimate": "string"}}

Return all content in Spanish.
